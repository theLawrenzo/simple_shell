#include "shell.c"

/**
 * _setenv - Modifies an environment variable
 * @name: environment variable to modify
 * @value: Value to add to the environment
 * @overwrite: Environment variable switch
 *
 * Return: 0 on success,
 * otherwise -1
 */
int _setenv(const char *name, const char *value, int overwrite)
{
}
