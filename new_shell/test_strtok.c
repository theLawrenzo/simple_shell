#include "shell.h"

/**
 * main - Tests the my_strtok function
 *
 * Return: Always 0
 */
/*
int main(void)
{
	char *env_var, **tokens, *sep = ":";
	int i;

	env_var = my_getenv("PATH");
	if (env_var == NULL)
	{
		printf("my_getenv function failed\n");
		exit(EXIT_FAILURE);
	}

	tokens = my_strtok(env_var, sep);
	if (tokens == NULL)
	{
		printf("my_strtok function failed\n");
		exit(EXIT_FAILURE);
	}

	for (i = 0; tokens[i] != NULL; i++)
		printf("%s\n", tokens[i]);

	return (0);
}
*/
